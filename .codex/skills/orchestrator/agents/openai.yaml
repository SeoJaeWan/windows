interface:
  display_name: "Orchestrator"
  short_description: "Run the planning loop through architect, review, browser gate, and materialize."
  default_prompt: "Use $orchestrator to orchestrate the explicit architect -> review -> revise -> mandatory browser-open user gate -> materialize planning workflow, recording whether materialization passed or failed its targeted gate."
