interface:
  display_name: "Architect"
  short_description: "Create controller-friendly plans after resolving blocking ambiguity."
  default_prompt: "Use $architect to resolve blocking ambiguity and produce controller-friendly plan summaries with an agreement table, overall work map, per-phase file-level change maps, and linked technical detail files under ./plans."
