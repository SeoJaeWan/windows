**Branch:** {type}/{task-slug}

> Worktree dir: `worktrees/{task-slug}` (plan 폴더명과 동일)
> 이 문서는 검토용 요약이다. 먼저 `사전 합의`와 `전체 작업 지도`에서 이번 변경의 기준과 흐름을 보고, 아래 phase 카드에서 어떤 파일이 어떻게 바뀌는지와 무엇을 확인해야 하는지를 본다.
> 기술적인 입력/출력 계약, owner_agent, 세부 작업, 검증은 각 phase의 상세 문서에서 다룬다.

# {작업명} 실행 계획

## 사전 합의

| 항목 | 합의 내용 | 적용 phase | 메모 |
| --- | --- | --- | --- |
| {이번 작업에서 미리 고정된 정책 또는 결정} | {왜 이 방향으로 고정되었는지까지 짧게 포함한 합의 내용} | {Phase 1 / Phase 2 / 전체} | {선택} |
| {이번 작업에서 미리 고정된 정책 또는 결정} | {왜 이 방향으로 고정되었는지까지 짧게 포함한 합의 내용} | {Phase 1 / Phase 2 / 전체} | {선택} |

## 전체 작업 지도

| Phase | 이번 단계에서 하는 일 | 끝나면 달라지는 상태 | 다음 단계로 넘기는 것 |
| --- | --- | --- | --- |
| Phase 1. {짧고 쉬운 역할 이름} | {이 phase의 실제 작업을 한 문장으로} | {끝나면 고정되는 상태를 한 문장으로} | {다음 phase가 바로 사용할 산출물 또는 조건} |
| Phase 2. {짧고 쉬운 역할 이름} | {이 phase의 실제 작업을 한 문장으로} | {끝나면 고정되는 상태를 한 문장으로} | {다음 phase가 바로 사용할 산출물 또는 조건} |
| Phase 3. {짧고 쉬운 역할 이름} | {이 phase의 실제 작업을 한 문장으로} | {끝나면 고정되는 상태를 한 문장으로} | {다음 phase가 바로 사용할 산출물 또는 조건} |

## 단계별 실행

### Phase 1. {짧고 쉬운 역할 이름}

- 목적:
- 변경 내용:
- 파일별 작업:

| 파일 | 작업 방식 | 기대 결과 | 완료 조건 |
| --- | --- | --- | --- |
| `path/to/file` | {추가/분리/교체/정리/검증} | {이 파일이 맡게 되는 새 역할 또는 바뀌는 계약} | {이 파일 기준으로 승인 가능한 상태} |
| `path/to/file` | {추가/분리/교체/정리/검증} | {이 파일이 맡게 되는 새 역할 또는 바뀌는 계약} | {이 파일 기준으로 승인 가능한 상태} |

- 이전 상태:
- 이후 상태:
- 완료 조건:
- 관련 영역:
- 시작 조건: `none` (선택)
- 상세: `./phases/01-{phase-slug}.md`

### Phase 2. {짧고 쉬운 역할 이름}

- 목적:
- 변경 내용:
- 파일별 작업:

| 파일 | 작업 방식 | 기대 결과 | 완료 조건 |
| --- | --- | --- | --- |
| `path/to/file` | {추가/분리/교체/정리/검증} | {이 파일이 맡게 되는 새 역할 또는 바뀌는 계약} | {이 파일 기준으로 승인 가능한 상태} |
| `path/to/file` | {추가/분리/교체/정리/검증} | {이 파일이 맡게 되는 새 역할 또는 바뀌는 계약} | {이 파일 기준으로 승인 가능한 상태} |

- 이전 상태:
- 이후 상태:
- 완료 조건:
- 관련 영역:
- 시작 조건: (선택)
- 상세: `./phases/02-{phase-slug}.md`

### Phase 3. {짧고 쉬운 역할 이름}

- 목적:
- 변경 내용:
- 파일별 작업:

| 파일 | 작업 방식 | 기대 결과 | 완료 조건 |
| --- | --- | --- | --- |
| `path/to/file` | {추가/분리/교체/정리/검증} | {이 파일이 맡게 되는 새 역할 또는 바뀌는 계약} | {이 파일 기준으로 승인 가능한 상태} |
| `path/to/file` | {추가/분리/교체/정리/검증} | {이 파일이 맡게 되는 새 역할 또는 바뀌는 계약} | {이 파일 기준으로 승인 가능한 상태} |

- 이전 상태:
- 이후 상태:
- 완료 조건:
- 관련 영역:
- 시작 조건: (선택)
- 상세: `./phases/03-{phase-slug}.md`
