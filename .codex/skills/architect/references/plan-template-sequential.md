**Branch:** {type}/{task-slug}

> Worktree dir: `worktrees/{task-slug}` (plan 폴더명과 동일)

# {작업명} 실행 계획

| # | Phase | Agent |
| --- | --- | --- |
| 1 | `./phases/01-{phase-slug}.md` | `{agent-name}` |
| 2 | `./phases/02-{phase-slug}.md` | `{agent-name}` |
| 3 | `./phases/03-{phase-slug}.md` | `{agent-name}` |

## 이번 작업 한눈에 보기

- 목표:
- 이번 계획의 핵심 변경:
- 완료되면 달라지는 점:
- 제외 범위: (선택)

## 사전 합의

| 항목 | 합의 내용 | 적용 범위 | 메모 |
| --- | --- | --- | --- |
| {고정 정책} | {왜 이 방향으로 고정되었는지까지 짧게} | {전체 / Phase 1 / Phase 2} | {선택} |
| {고정 정책} | {왜 이 방향으로 고정되었는지까지 짧게} | {전체 / Phase 1 / Phase 2} | {선택} |

## Phase 흐름 요약

| Phase | 역할 | 하는 일 | 끝나면 고정되는 상태 | 다음 단계 인계 |
| --- | --- | --- | --- | --- |
| Phase 1. {이름} | {짧은 역할} | {무엇을 정리/구축/검증하는지} | {이 단계 종료 시 확정되는 상태} | {산출물/조건} |
| Phase 2. {이름} | {짧은 역할} | {무엇을 연결/적용/확장하는지} | {이 단계 종료 시 확정되는 상태} | {산출물/조건} |
| Phase 3. {이름} | {짧은 역할} | {무엇을 검증/마감/정리하는지} | {이 단계 종료 시 확정되는 상태} | {산출물/조건} |

## 단계별 실행

### Phase 1. {짧고 쉬운 역할 이름}

- 목적:
- 왜 먼저 하는가:
- 시작 조건: `none` (선택)
- 핵심 변경:
- 완료 조건:
- 다음 단계로 넘기는 것:
- 상세 문서: `./phases/01-{phase-slug}.md`

### Phase 2. {짧고 쉬운 역할 이름}

- 목적:
- 왜 이 단계가 필요한가:
- 시작 조건:
- 핵심 변경:
- 완료 조건:
- 다음 단계로 넘기는 것:
- 상세 문서: `./phases/02-{phase-slug}.md`

### Phase 3. {짧고 쉬운 역할 이름}

- 목적:
- 왜 마지막 단계인가:
- 시작 조건:
- 핵심 변경:
- 완료 조건:
- 최종 산출물:
- 상세 문서: `./phases/03-{phase-slug}.md`

## 체크포인트

- [ ] Phase 1 완료 시 `{고정되어야 하는 상태}`
- [ ] Phase 2 완료 시 `{연결 또는 적용 상태}`
- [ ] Phase 3 완료 시 `{최종 검증 또는 배포 가능 상태}`
