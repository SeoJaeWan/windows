# Phase 1. 바와 기본 조작 화면 옮기기

> 이 문서는 실행 계약이다. `plan.md`의 같은 phase 요약을 기술적으로 확장하되, 범위와 완료 조건을 바꾸지 않는다.

- owner_agent: `frontend-developer`
- 목적: `https://seojaewan.com`과 `C:/Users/USER/Desktop/dev/blog`의 작업 시점 하단 taskbar 인상을 package 내부 토큰과 기본 스타일로 옮긴다.
- boundary:
  - `packages/tailwind-config/src/theme.css`
  - `packages/tailwind-config/src/utilities.css`
  - `packages/ui/src/components/taskbar/taskbar/**`
  - `packages/ui/src/components/taskbar/taskbarSearch/**`
  - `packages/ui/src/components/taskbar/taskbarIconButton/**`
  - `packages/ui/src/components/taskbar/taskbarClock/**`
  - `packages/ui/src/components/taskbar/internal/searchField/**`
  - `packages/ui/src/components/taskbar/internal/icon/**`
- input:
  - `plans/windows-taskbar-work-02-composite-assembly/phases/03-web-sandbox-alignment.md`에서 package public surface와 surviving web sandbox consumer가 모두 새 `Taskbar` 계약으로 맞춰진 상태
  - `C:/Users/USER/Desktop/dev/blog/src/app/globals.css`
  - `C:/Users/USER/Desktop/dev/blog/src/components/templates/bottomBar/index.tsx`
  - `C:/Users/USER/Desktop/dev/blog/src/components/atoms/taskInput/index.tsx`
  - `https://seojaewan.com` 2026-04-08 기준 홈 화면 taskbar 인상
- output:
  - 공개 계약:
    - `Taskbar`, `TaskbarSearch`, `TaskbarIconButton`, `TaskbarClock`는 package 내부 토큰과 기본 스타일만으로 bar/search/icon/clock의 기본 인상을 렌더링한다.
    - 이 phase는 기존 컴포넌트 API 안에서 시각 책임을 package로 되돌리는 작업이며, 소비자용 새 selector나 class naming contract를 추가하지 않는다.
  - 내부 기본값:
    - 유리감, 배경, 테두리, 그림자, 글자 톤, 검색창 강조는 `@windows/tailwind-config`와 component class가 source of truth다.
    - 구현 중 선택한 관찰 anchor(class, token, data-attribute)는 package 내부 검증용일 뿐 public API로 승격하지 않는다.
    - consumer `className`은 additive override일 뿐이고 기본 시각 책임을 package 밖으로 밀어내지 않는다.
    - 참조 사이트와 블로그는 작업 시점 참고 자료이며, 이후 자동 동기화 대상은 아니다.
  - 허용하지 않는 대안:
    - app/web route wrapper, global CSS, preview-only selector가 있어야만 참조 look이 성립하는 구조
    - Next font API, route-local wallpaper, preview 전용 prop을 package public contract에 여는 구조
- 선행조건: `plans/windows-taskbar-work-02-composite-assembly/phases/03-web-sandbox-alignment.md` 완료
- 제약:
  - viewport anchoring과 route layout은 범위 밖이다.
  - 이 phase는 시각 ownership을 닫는 단계이지 browser state를 추가하는 단계가 아니다.
  - 외부 reference의 exact class 이름, pixel-perfect 복제, 이후 live sync는 범위 밖이다.
- failure/validation: package가 아니라 app/web globals나 route wrapper가 bar/search/icon/clock 인상의 대부분을 책임지게 되면 실패다.
- 작업:
  - 작업 시점 reference에서 유리감, 테두리, 그림자, 글자 톤, 검색창 강조의 핵심 인상을 뽑아 `--taskbar-*` 토큰과 component 기본 스타일로 옮긴다.
  - `Taskbar`, search, icon button, clock의 시각 책임을 package 경계 안으로 모은다.
  - additive `className` 규칙은 유지하되 custom class가 없어도 기본 인상이 성립하도록 정리한다.
- 검증:
  - [ ] `pnpm --filter @windows/ui test`
  - [ ] `pnpm --filter @windows/ui exec tsc --noEmit -p tsconfig.json`
  - [ ] `pnpm --filter @windows/web build`
  - [ ] 코드 검색으로 bar/search/icon/clock의 토큰 또는 시각 anchor가 `packages/tailwind-config`와 `packages/ui/src/components/taskbar/**` 안에 모여 있고 `apps/web` route 파일이 같은 책임을 대신하지 않는지 확인한다.
  - [ ] custom className을 제거한 기본 렌더 기준으로도 bar/search/icon/clock 인상이 무너지지 않는지 확인한다.
  - [ ] 작업 시점 reference와 로컬 렌더 결과를 눈으로 비교해 유리감, 테두리/그림자, 간격, 글자 톤이 같은 계열인지 확인한다. exact class명이나 pixel-perfect 일치는 요구하지 않는다.
