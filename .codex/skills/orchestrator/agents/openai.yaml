interface:
  display_name: "Orchestrator"
  short_description: "Run architect-review-materialize with mandatory browser gate."
  default_prompt: "Use $orchestrator to orchestrate the explicit architect -> review -> revise -> mandatory browser-open user gate -> materialize planning workflow with named planning agents only."
