/**
 * Folder — windows family leaf component (stub)
 *
 * Public export declared in packages/ui/src/index.ts.
 * Concrete implementation is Phase 3+ work.
 *
 * Foundation contract (Phase 2):
 *   - Uses WindowFrame as shared shell owner (internal).
 *   - Chrome variant: tab-bar style titlebar (leaf-specific, not shared).
 *   - Mobile hierarchy: content-first grid.
 *     Sidebar collapses to drawer; grid items fill the viewport.
 *     Desktop sidebar shrink is NOT a valid mobile implementation.
 *   - Token namespace: --window-* (no --panel-* or raw literals).
 */
function Folder() {
  return null;
}

export default Folder;
